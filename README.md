# cnx-svelte-demo

```bash
npm install
npm run dev
```

Navigate to [localhost:5000](http://localhost:5000).

Optional Query Params:

- `playerId`
- `lineItemId`
- `videoType=(vast|hls)`
- `videoUrl`
